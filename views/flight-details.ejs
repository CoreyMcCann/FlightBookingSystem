<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <title>Flight Details</title>
</head>

<body>
    <div class="container mt-5">
        <!-- Back Button -->
        <a href="/search-results?from=<%= flight.origin.name %>&to=<%= flight.destination.name %>&departure=<%= flight.departureTime.toISOString().split('T')[0] %>"
            class="btn btn-secondary mb-3">&larr; Back</a>

        <!-- Title -->
        <h1 class="text-center mb-4">Flight Details</h1>

        <!-- Flight Details Card -->
        <div class="card mx-auto" style="max-width: 600px;">
            <div class="card-body">
                <h5 class="card-title text-center">
                    <%= flight.airline %>
                </h5>
                <div class="d-flex justify-content-between mb-3">
                    <div>
                        <strong>From:</strong>
                        <%= flight.origin.name %> (<%= flight.origin.code %>)
                    </div>
                    <div>
                        <strong>To:</strong>
                        <%= flight.destination.name %> (<%= flight.destination.code %>)
                    </div>
                </div>
                <div class="d-flex justify-content-between mb-3">
                    <div>
                        <strong>Date:</strong>
                        <%= new Date(flight.departureTime).toLocaleDateString() %>
                    </div>
                    <div>
                        <strong>Time:</strong>
                        <%= new Date(flight.departureTime).toLocaleTimeString([], { hour: '2-digit' , minute: '2-digit'
                            }) %>
                    </div>
                </div>
                <div class="text-center">
                    <h4>Price: $<%= flight.price %>
                    </h4>
                </div>
            </div>
        </div>

        <!-- Choose Seats Button -->
        <div class="text-center mt-4">
            <a href="/choose-seats/<%= flight.id %>" class="btn btn-primary">Choose Seats</a>
        </div>
    </div>

    <!-- Bootstrap JavaScript -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>